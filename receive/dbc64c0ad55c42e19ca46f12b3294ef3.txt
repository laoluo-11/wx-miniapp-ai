请基于Python Flask开发极简协议中转Demo，适配：本地Hermes(接入DeepSeek) ↔ 小艺开放平台A2A，代码轻量化、Ubuntu一键部署、先本地curl自测通。
硬性需求：
1. 对外接口固定 POST /agent/message，完全实现华为小艺云A2A 1.0全套规范
   - 完整AK/SK HmacSHA256签名鉴权，请求头携带accessKey、ts(毫秒)、sign，时间戳允许15分钟误差，非法请求返回401
   - 严格解析A2A标准入参：sessionId会话ID、用户提问文本
   - 输出强制匹配A2A标准JSON：version、session、reply三层结构
2. 内部对接本机Hermes
   - Hermes默认OpenAI兼容接口地址 http://127.0.0.1:8080/v1/chat/completions
   - 读取A2A会话历史，拼接上下文传给Hermes，实现多轮对话记忆
   - 提取Hermes返回文本，封装成小艺A2A格式返回
3. 轻量化会话存储
   使用本地SQLite存储session对话上下文，无Redis/MySQL重型依赖；会话7天无交互自动清理
4. 统一配置区
   文件顶部集中所有可变参数：小艺AK/SK、Hermes本地地址、服务端口8000
5. 配套产出文件
   1) app.py 完整中转代码，详细分层注释（A2A鉴权层、Hermes调用层、会话存储层）
   2) requirements.txt 仅必要依赖
   3) systemd服务配置文件，后台常驻、开机自启
   4) Nginx HTTPS反向代理配置模板（适配Let’s Encrypt域名证书）
6. 配套操作内容
   - Ubuntu全套部署命令（安装依赖、初始化数据库、启停服务、查看日志）
   - curl本地自测脚本，直接测试/agent/message接口连通性
   - 小艺开放平台参数填写说明
   - 常见报错排查清单（签名失败、Hermes连不上、平台超时、格式错误）
输出顺序：项目目录结构 → app.py完整代码 → 依赖文件 → systemd配置 → nginx配置 → 部署命令 → curl测试脚本 → 对接平台说明
